# URL del Backend API (utilizada por el proxy de Nginx para redirigir /api-proxy/ al backend)
# IMPORTANTE: NO debe terminar en barra "/" — nginx la agrega automáticamente en la configuración
BACKEND_URL=https://api.tu-servidor.com
